{% spaceless %}
    {% if schema is not null %}
        <script type="application/ld+json">
            {{ schema|json_encode(constant('JSON_UNESCAPED_SLASHES'))|raw }}
        </script>
    {% endif %}
{% endspaceless %}